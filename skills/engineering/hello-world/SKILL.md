---
name: hello-world
description: A minimal stub skill that always responds with 'hello world' — used to verify this repo's skills are discoverable via `npx skills add`.
disable-model-invocation: true
---

When invoked, respond with the literal text `hello world` and nothing else.
